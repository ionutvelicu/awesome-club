---
import App from '../../components/dashboard/App';
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout title="Courses">
  <App client:only />
 <!--- <VideoPlayer
      src="/video/playlist.m3u8"
      controls
      client:only="react"
      onStop={({ currentTime, duration, reason }) => {
        // save locally
        localStorage.setItem(`lastpos:${location.pathname}`, String(currentTime));
        // POST to server
        console.log("Stopped:", { currentTime, duration, reason });
      }}
  /> --->
</BaseLayout>
